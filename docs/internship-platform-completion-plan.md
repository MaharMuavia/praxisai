# Internship platform completion remediation plan

This plan records the release-critical gaps found at `8161ae259742a9a35c6930295c1c873e271bad83` and the implemented remediation on `agent/internship-platform-completion`.

| Severity | Current behavior | User impact | Security/privacy impact | Root cause | Target architecture | Files changed | Tests | Verification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Critical | Internship migration calls ORM `create_all`/`drop_all`. | Deployment success depends on metadata and downgrade safety is uncertain. | Schema drift can omit foreign keys and constraints. | Migration was used as a metadata shortcut. | Explicit Alembic table, index, constraint, FK, and reverse-order operations. | `apps/api/alembic/versions/d1e2f3a4b5c6_add_internship_learning_platform.py` | Migration upgrade/downgrade and drift checks. | Pending full environment run. |
| High | Admission counts enrollments without locking the cohort or track capacity row. | Concurrent accepts can over-enroll a cohort. | Capacity policy is not authoritative under concurrency. | Transaction only locks the application. | Lock cohort and cohort-track rows before counting and rely on unique enrollment constraint. | `apps/api/app/internships/service.py`, migration | PostgreSQL last-seat concurrency test. | Pending full PostgreSQL run. |
| High | Operations routes use a shared role dependency; reviewer/technical-lead routes are broad. | Staff can see or attempt actions outside their assignment. | Least-privilege and student privacy boundaries are weakened. | Authorization is role-based rather than capability and record scoped. | Endpoint-specific capability dependencies plus assigned-review scoping. | `apps/api/app/auth/dependencies.py`, `apps/api/app/api/auth.py`, internship routes | Complete capability matrix. | Pending full test run. |
| High | Upload route reads `request.body()` and Supabase uploads become `CLEAN` after hash verification. | Large uploads consume API memory; scan failures are not represented. | Unscanned production objects may be attached to submissions. | Storage and scanning were coupled to a synchronous byte-buffer path. | Direct/scoped storage target, explicit scan states, production scanner boundary, quarantine on disabled scanner. | `apps/api/app/internships/service.py`, `storage.py`, upload route | MIME, magic-byte, quarantine, malware, ZIP-bomb tests. | Pending provider-backed run. |
| High | Submission hash includes upload IDs, not immutable evidence metadata; artifact ownership/state is not fully checked. | Finalization can bind to changed or invalid artifacts. | Evidence integrity and auditability are incomplete. | Finalization validates presence, not a frozen artifact snapshot. | Lock assignment/submission, validate every upload, freeze metadata, hash canonical evidence. | `apps/api/app/internships/service.py`, models, migration | Canonical hash and finalization idempotency tests. | Pending full test run. |
| High | Rubric scorer accepts duplicate/missing criteria; below-threshold PASS is converted to FAIL. | Reviewers can submit invalid rubrics and decisions do not match requested outcome. | Human decision record is internally inconsistent. | Scoring checks only seen rows and silently repairs decisions. | Exact criterion set, deterministic weighted score, reject inconsistent PASS. | `apps/api/app/internships/service.py` | Duplicate, missing, unknown, and threshold tests. | Pending full test run. |
| Medium | Resubmission limit is hardcoded to three attempts. | Programs cannot configure policy. | Policy changes cannot be applied consistently. | Policy is stored but not interpreted. | Assignment/cohort policy evaluator with deadline and competing-draft checks. | `apps/api/app/internships/service.py` | Configurable resubmission tests. | Pending full test run. |
| Medium | Dashboard timeline marks application and admission complete by position. | Students see progress that may not reflect backend state. | UI can misrepresent a decision or completion state. | Timeline was assembled with fallback labels. | Backend-owned timeline items for every application, phase, assignment, completion, and credential state. | `apps/api/app/internships/service.py`, schemas, student UI | Timeline truthfulness tests. | Pending full test run. |
| Medium | Internship certificate is a parallel payload instead of the signed `Credential` record. | Credential evidence is split across two systems. | Public credential privacy/signing guarantees are inconsistent. | Internship vertical slice introduced its own certificate table. | Completion approval feeds the existing signed credential lifecycle; internship record is workflow state only. | `apps/api/app/internships/credentials.py`, `apps/api/app/credentials/lifecycle.py`, models | Signed credential, privacy, revocation tests. | Pending full test run. |
| Medium | Notifications, retention, and analytics are partial or documentation-only. | Staff lack operational visibility and lifecycle automation. | Retention obligations are not executable. | Vertical slice stopped at presentation and core transitions. | Outbox events, bounded idempotent jobs, authoritative aggregate queries. | `apps/api/app/outbox`, `operations`, internship modules | Worker idempotency and analytics tests. | Pending full test run. |

## Verification policy

Every row remains `Pending` until the relevant command has actually run against the intended database/provider configuration. A green unit test that uses `Base.metadata.create_all` does not verify the Alembic migration contract.
