import { WorkspaceRoute } from "@/components/workspace/workspace-route";
export default function UniversitySettingsPage() {
  return (
    <WorkspaceRoute
      path="/university/settings"
      title="Settings"
      description="Review institutional notification and reporting settings."
    />
  );
}
